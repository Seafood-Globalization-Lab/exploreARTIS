library(testthat)         # Loads the testing framework
library(exploreARTIS)     # Loads your package so you can test its functions
test_check("exploreARTIS")  # Finds and runs all test-* files in tests/testthat/
